apiVersion: apps/v1
kind: Deployment
metadata:
  name: alisio
  labels:
    app: alisio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alisio
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: alisio
    spec:
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: init-config
          image: busybox:1.37
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              cp /config/alisio.json /home/node/.alisio/alisio.json
              mkdir -p /home/node/.alisio/workspace
              cp /config/AGENTS.md /home/node/.alisio/workspace/AGENTS.md
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          resources:
            requests:
              memory: 32Mi
              cpu: 50m
            limits:
              memory: 64Mi
              cpu: 100m
          volumeMounts:
            - name: alisio-home
              mountPath: /home/node/.alisio
            - name: config
              mountPath: /config
      containers:
        - name: gateway
          image: ghcr.io/alisio/alisio:slim
          imagePullPolicy: IfNotPresent
          command:
            - node
            - /app/dist/index.js
            - gateway
            - run
          ports:
            - name: gateway
              containerPort: 40705
              protocol: TCP
          env:
            - name: HOME
              value: /home/node
            - name: ALISIO_CONFIG_DIR
              value: /home/node/.alisio
            - name: NODE_ENV
              value: production
            - name: ALISIO_GATEWAY_TOKEN
              valueFrom:
                secretKeyRef:
                  name: alisio-secrets
                  key: ALISIO_GATEWAY_TOKEN
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: alisio-secrets
                  key: ANTHROPIC_API_KEY
                  optional: true
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: alisio-secrets
                  key: OPENAI_API_KEY
                  optional: true
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: alisio-secrets
                  key: GEMINI_API_KEY
                  optional: true
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: alisio-secrets
                  key: OPENROUTER_API_KEY
                  optional: true
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 2Gi
              cpu: "1"
          livenessProbe:
            exec:
              command:
                - node
                - -e
                - "require('http').get('http://127.0.0.1:40705/healthz', r => process.exit(r.statusCode < 400 ? 0 : 1)).on('error', () => process.exit(1))"
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            exec:
              command:
                - node
                - -e
                - "require('http').get('http://127.0.0.1:40705/readyz', r => process.exit(r.statusCode < 400 ? 0 : 1)).on('error', () => process.exit(1))"
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: alisio-home
              mountPath: /home/node/.alisio
            - name: tmp-volume
              mountPath: /tmp
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: alisio-home
          persistentVolumeClaim:
            claimName: alisio-home-pvc
        - name: config
          configMap:
            name: alisio-config
        - name: tmp-volume
          emptyDir: {}
