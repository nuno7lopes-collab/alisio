apiVersion: v1
kind: Service
metadata:
  name: alisio
  labels:
    app: alisio
spec:
  type: ClusterIP
  selector:
    app: alisio
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
